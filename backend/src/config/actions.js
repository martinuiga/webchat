const socketActions = {
    INITIALIZE: 'server/initialize',
    SEND_MESSAGE: 'server/send_message',
    RECONNECT: 'server/reconnect',
    TYPING: 'server/typing',
    SEND_DEAL: 'server/send_pipedrive_deal'
};

module.exports = socketActions;
